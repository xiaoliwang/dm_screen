<input type='file' id="pic" />
<input id="sendNews"/>
<input id="click" type = "button" value = "确认"/>
<script>
$(function(){
    var socket = io('/backend');

    //发送News
    $('#click').click(function(){
        socket.emit('NEWS', $('#sendNews').val());
        $('#sendNews').val('');
    });

    //发送pic
    $("#pic").change(function(){
        readImage( this );
        this.value = "";
    });

    function readImage(input) {
        if ( input.files && input.files[0] ) {
            var FR= new FileReader();
            FR.onload = function(e) {
                socket.emit('PIC',e.target.result);
            };
            FR.readAsDataURL( input.files[0] );
        }
    }

});

</script>
